<?xml version="1.0" encoding="utf-8"?>
<templates>
    <t t-name="meeting_management_base.MeetingSessionView">
        <div class="dw-meeting-view" role="main">
            <!-- Header -->
            <header class="meeting-header">
                <div class="meeting-header-left">
                    <button class="btn-back" t-on-click="goBack" title="Back">
                        <i class="fa fa-arrow-left"/>
                    </button>
                    <div class="meeting-title-section">
                        <h1 class="meeting-title">
                            <t t-esc="state.session.name"/>
                        </h1>
                        <div class="meeting-meta-info">
                            <span class="meta-badge" t-if="state.session.is_host">
                                <i class="fa fa-star"/>
                                Host
                            </span>
                        </div>
                    </div>
                </div>
                <div class="meeting-header-right">
                    <span class="status-chip" t-att-class="state.session.state">
                        <span class="status-dot"/>
                        <t t-esc="state.session.state.charAt(0).toUpperCase() + state.session.state.slice(1).replace('_', ' ')"/>
                    </span>
                    <button class="btn-action danger" t-if="state.session.state !== 'done'" t-on-click="leaveMeeting"
                            title="Leave">
                        <i class="fa fa-sign-out"/>
                    </button>
                    <button class="btn-action danger" t-if="state.session.is_host and state.session.state !== 'done'"
                            t-on-click="endMeeting"
                            title="End Meeting for All">
                        <i class="fa fa-power-off"/>
                    </button>
                </div>
            </header>

            <div class="meeting-content-grid">
                <!-- Left Sidebar: Participants -->
                <aside class="meeting-sidebar meeting-sidebar-left">
                    <div class="sidebar-card">
                        <div class="card-header">
                            <h3>
                                <i class="fa fa-users"/>
                                Participants (<t t-esc="state.session.participants?.length"/>)
                            </h3>
                        </div>
                        <div class="card-body">
                            <div class="participants-list">
                                <t t-if="state.session.participants?.length">
                                    <t t-foreach="state.session.participants" t-as="p" t-key="p_index">
                                        <div class="participant-item">
                                            <div class="participant-avatar">
                                                <t t-esc="p.name.slice(0, 2).toUpperCase()"/>

                                                <span t-att-class="'status-badge status-' + (p.attendance_status || 'default')"
                                                      t-att-title="getStatusLabel(p.attendance_status)"
                                                      t-if="state.session.state !== 'done'"
                                                />
                                            </div>
                                            <div class="participant-info">
                                                <div class="participant-name">
                                                    <t t-esc="p.name"/>
                                                </div>
                                            </div>
                                            <div class="participant-status">
                                                <t t-if="p.attendance_status === 'present'">
                                                    <i class="fa fa-circle text-success"/>
                                                    Present
                                                </t>
                                                <t t-elif="p.attendance_status === 'pause'">
                                                    <i class="fa fa-circle text-warning"/>
                                                    Paused
                                                </t>
                                                <t t-elif="p.attendance_status === 'absent'">
                                                    <i class="fa fa-circle text-danger"/>
                                                    Absent
                                                </t>
                                                <t t-elif="p.attendance_status === 'excused'">
                                                    <i class="fa fa-circle text-muted"/>
                                                    Excused
                                                </t>
                                                <t t-else="">
                                                    <i class="fa fa-circle text-secondary"/>
                                                    Awaiting
                                                </t>
                                            </div>
                                        </div>
                                    </t>
                                </t>
                                <t t-else="">
                                    <div class="empty-participants">
                                        <i class="fa fa-users"/>
                                        <p>No participants</p>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </div>

                    <!-- Waiting Participants -->
                    <div class="sidebar-card" t-if="state.session.is_host &amp;&amp; state.waitingParticipants.length">
                        <div class="card-header">
                            <h3>
                                <i class="fa fa-hourglass"/>
                                Waiting (<t t-esc="state.waitingParticipants.length"/>)
                            </h3>
                        </div>
                        <div class="card-body">
                            <div class="participants-list">
                                <t t-foreach="state.waitingParticipants" t-as="w" t-key="w.id">
                                    <div class="participant-item">
                                        <div class="participant-avatar">
                                            <t t-esc="w.name?.slice(0, 2).toUpperCase()"/>
                                        </div>
                                        <div class="participant-info">
                                            <div class="participant-name">
                                                <t t-esc="w.name"/>
                                            </div>
                                        </div>
                                        <div style="display:flex; gap:4px;">
                                            <button class="btn-icon-sm"
                                                    t-on-click="() => admitParticipant(w.id)"
                                                    title="Admit">
                                                <i class="fa fa-check" style="color: #10b981;"/>
                                            </button>
                                            <button class="btn-icon-sm"
                                                    t-on-click="() => rejectParticipant(w.id)"
                                                    title="Reject">
                                                <i class="fa fa-times" style="color: #ef4444;"/>
                                            </button>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </div>

                    <!-- Meeting Info -->
                    <div class="sidebar-card">
                        <div class="card-header">
                            <h3>
                                <i class="fa fa-info-circle"/>
                                Details
                            </h3>
                        </div>
                        <div class="card-body">
                            <div class="info-list">
                                <div class="info-item" t-if="state.session.objet">
                                    <label>Subject</label>
                                    <span t-esc="state.session.objet"/>
                                </div>
                                <div class="info-item" t-if="state.session.meeting_type_id">
                                    <label>Type</label>
                                    <span t-esc="state.meetingTypeName"/>
                                </div>
                                <div class="info-item" t-if="state.session.planned_start_datetime">
                                    <label>Planned start</label>
                                    <span t-esc="state.session.planned_start_datetime"/>
                                </div>
                                <div class="info-item" t-if="state.session.planned_end_time">
                                    <label>Planned end</label>
                                    <span t-esc="state.session.planned_end_time"/>
                                </div>
                                <div class="info-item">
                                    <label>Planned Duration</label>
                                    <span>
                                        <t t-esc="state.sessionDuration"/>
                                        hours
                                    </span>
                                </div>
                                <div class="info-item">
                                    <label>Actual start</label>
                                    <span>
                                        <t t-esc="state.session.actual_start_datetime"/>
                                    </span>
                                </div>
                                <div class="info-item">
                                    <label>Elapsed Time</label>
                                    <span t-esc="state.meetingDuration"/>
                                </div>
                                <div class="info-item" t-if="state.session.join_datetime">
                                    <label>Joined At</label>
                                    <span t-esc="state.formattedJoinTime"/>
                                </div>
                            </div>
                        </div>
                    </div>
                </aside>

                <!-- Main Content -->
                <main class="meeting-main">
                    <!-- Tab Navigation -->
                    <div class="main-tab-navigation">
                        <button class="tab-nav-button"
                                t-att-class="{ 'active': state.activeMainTab === 'agenda' }"
                                t-on-click="() => onTabChange('agenda')">
                            <i class="fa fa-file-text-o"/>
                            <span>Agenda</span>
                        </button>
                        <button class="tab-nav-button"
                                t-att-class="{ 'active': state.activeMainTab === 'actions' }"
                                t-if="state.session.is_action_assigner"
                                t-on-click="() => onTabChange('actions')">
                            <i class="fa fa-tasks"/>
                            <span>Actions (<t t-esc="state.actions.length"/>)
                            </span>
                        </button>
                        <button class="tab-nav-button"
                                t-att-class="{ 'active': state.activeMainTab === 'notes' }"
                                t-on-click="() => onTabChange('notes')">
                            <i class="fa fa-file-text-o"/>
                            <span>My Notes</span>
                        </button>
                        <button class="tab-nav-button"
                                t-att-class="{ 'active': state.activeMainTab === 'pv' }"
                                t-if="state.session.is_pv"
                                t-on-click="() => onTabChange('pv')">
                            <i class="fa fa-file-text-o"/>
                            <span>PV</span>
                        </button>
                        <button class="tab-nav-button"
                                t-att-class="{ 'active': state.activeMainTab === 'video' }"
                                t-if="state.session.state !== 'done' and state.session.use_vc"
                                t-on-click="() => onTabChange('video')">
                            <i class="fa fa-video-camera"/>
                            <span>Video Conference</span>
                        </button>
                    </div>

                    <!-- REPLACE this section in your meeting_session_view.xml -->
                    <!-- Change from t-if to always render, visibility controlled by CSS -->

                    <div class="video-conference-container"
                         t-if="state.session.state !== 'done' and state.session.use_vc"
                         t-att-class="{ 'hidden': !state.session.display_camera }">
                        <div id="jitsi-meet-container" class="jitsi-container"/>

                        <!-- Loading -->
                        <div class="video-loading" t-if="state.loading &amp; !state.error">
                            <div class="loading-spinner">
                                <div class="spinner-ring"/>
                            </div>
                        </div>
                    </div>

                    <!-- AGENDA Section - NEW -->
                    <div class="main-tab-content" t-if="state.activeMainTab === 'agenda'">
                        <div class="agenda-section">
                            <div class="agenda-header">
                                <h3>
                                    <i class="fa fa-list-ol"/>
                                    Ordre du Jour
                                </h3>
                                <div class="agenda-header-actions" t-if="state.session.can_edit_agenda">
                                    <div class="add-agenda-form">
                                        <input
                                                type="text"
                                                class="agenda-input"
                                                placeholder="Nouveau point à l'ordre du jour..."
                                                t-model="state.newAgendaName"
                                                t-on-keyup="(ev) => ev.key === 'Enter' &amp; addAgendaItem()"/>
                                        <button class="btn-add-agenda" t-on-click="addAgendaItem">
                                            <i class="fa fa-plus"/>
                                            Ajouter
                                        </button>
                                    </div>
                                </div>
                            </div>

                            <div class="agenda-list">
                                <t t-if="state.agendaItems?.length">
                                    <t t-foreach="state.agendaItems" t-as="agenda" t-key="agenda.id">
                                        <div class="agenda-item"
                                             t-att-class="getTimerStateClass(agenda.timer_state)"
                                             draggable="true"
                                             t-on-dragstart="(ev) => onAgendaDragStart(ev, agenda.id)"
                                             t-on-dragover="onAgendaDragOver"
                                             t-on-drop="(ev) => onAgendaDrop(ev, agenda.id)"
                                             t-on-dragend="onAgendaDragEnd">

                                            <!-- Agenda Item Header -->
                                            <div class="agenda-item-header">
                                                <div class="agenda-drag-handle">
                                                    <i class="fa fa-bars"/>
                                                </div>
                                                <div class="agenda-sequence">
                                                    <t t-esc="agenda_index + 1"/>
                                                </div>
                                                <div class="agenda-title-section">
                                                    <t t-if="state.editingAgendaId === agenda.id">
                                                        <input
                                                                type="text"
                                                                class="agenda-title-input"
                                                                t-model="agenda.name"
                                                                t-on-keyup="(ev) => ev.key === 'Enter' ? saveAgendaEdit(agenda.id) : ev.key === 'Escape' ? cancelAgendaEdit() : null"/>
                                                        <button class="btn-icon-sm" t-on-click="() => saveAgendaEdit(agenda.id)">
                                                            <i class="fa fa-check text-success"/>
                                                        </button>
                                                        <button class="btn-icon-sm" t-on-click="cancelAgendaEdit">
                                                            <i class="fa fa-times text-danger"/>
                                                        </button>
                                                    </t>
                                                    <t t-else="">
                                                        <h4 class="agenda-title">
                                                            <i t-att-class="'fa ' + getTimerStateIcon(agenda.timer_state)"/>
                                                            <t t-esc="agenda.name"/>
                                                        </h4>
                                                        <button class="btn-icon-sm"
                                                                t-if="state.session.can_edit_agenda"
                                                                t-on-click="() => startEditAgenda(agenda.id)">
                                                            <i class="fa fa-pencil"/>
                                                        </button>
                                                    </t>
                                                </div>
                                                <div class="agenda-actions">
                                                    <button class="btn-icon-sm danger"
                                                            t-if="state.session.can_edit_agenda"
                                                            t-on-click="() => deleteAgendaItem(agenda.id)">
                                                        <i class="fa fa-trash"/>
                                                    </button>
                                                </div>
                                            </div>

                                            <!-- Timer Section -->
                                            <div class="agenda-timer-section">
                                                <div class="timer-info">
                                                    <div class="timer-duration">
                                                        <label>Durée prévue:</label>
                                                        <t t-if="state.session.can_edit_agenda">
                                                            <input
                                                                    type="number"
                                                                    class="duration-input"
                                                                    min="1"
                                                                    max="240"
                                                                    t-att-value="agenda.duration_minutes"
                                                                    t-on-change="(ev) => updateAgendaItem(agenda.id, 'duration_minutes', parseInt(ev.target.value))"/>
                                                            <span>min</span>
                                                        </t>
                                                        <t t-else="">
                                                            <span class="duration-display">
                                                                <t t-esc="agenda.duration_minutes"/> min
                                                            </span>
                                                        </t>
                                                    </div>
                                                    <div class="timer-stats">
                                                        <div class="timer-stat">
                                                            <label>Écoulé:</label>
                                                            <span class="timer-value" t-att-data-agenda-elapsed="agenda.id">
                                                                <t t-esc="formatTime(agenda.elapsed_seconds || 0)"/>
                                                            </span>
                                                        </div>
                                                        <div class="timer-stat">
                                                            <label>Restant:</label>
                                                            <span class="timer-value" t-att-data-agenda-remaining="agenda.id">
                                                                <t t-esc="formatTime(Math.max(0, agenda.duration_minutes * 60 - (agenda.elapsed_seconds || 0)))"/>
                                                            </span>
                                                        </div>
                                                    </div>
                                                </div>

                                                <!-- Progress Bar -->
                                                <div class="timer-progress-container">
                                                    <div class="timer-progress-bar"
                                                         t-att-data-agenda-progress="agenda.id"
                                                         t-att-style="'width: ' + Math.min(100, ((agenda.elapsed_seconds || 0) / (agenda.duration_minutes * 60)) * 100) + '%'">
                                                    </div>
                                                </div>

                                                <!-- Timer Controls -->
                                                <div class="timer-controls">
                                                    <t t-if="agenda.timer_state === 'not_started'">
                                                        <button class="btn-timer start" t-on-click="() => startAgendaTimer(agenda.id)">
                                                            <i class="fa fa-play"/>
                                                            Démarrer
                                                        </button>
                                                    </t>
                                                    <t t-elif="agenda.timer_state === 'running' or agenda.timer_state === 'overtime'">
                                                        <button class="btn-timer pause" t-on-click="() => pauseAgendaTimer(agenda.id)">
                                                            <i class="fa fa-pause"/>
                                                            Pause
                                                        </button>
                                                        <button class="btn-timer stop" t-on-click="() => stopAgendaTimer(agenda.id)">
                                                            <i class="fa fa-stop"/>
                                                            Terminer
                                                        </button>
                                                    </t>
                                                    <t t-elif="agenda.timer_state === 'paused'">
                                                        <button class="btn-timer resume" t-on-click="() => startAgendaTimer(agenda.id)">
                                                            <i class="fa fa-play"/>
                                                            Reprendre
                                                        </button>
                                                        <button class="btn-timer stop" t-on-click="() => stopAgendaTimer(agenda.id)">
                                                            <i class="fa fa-stop"/>
                                                            Terminer
                                                        </button>
                                                    </t>
                                                    <t t-elif="agenda.timer_state === 'completed'">
                                                        <button class="btn-timer reset" t-on-click="() => resetAgendaTimer(agenda.id)">
                                                            <i class="fa fa-refresh"/>
                                                            Réinitialiser
                                                        </button>
                                                    </t>
                                                </div>

                                                <!-- Timer State Badge -->
                                                <div class="timer-state-badge" t-att-class="agenda.timer_state">
                                                    <t t-if="agenda.timer_state === 'not_started'">Pas démarré</t>
                                                    <t t-elif="agenda.timer_state === 'running'">En cours</t>
                                                    <t t-elif="agenda.timer_state === 'paused'">En pause</t>
                                                    <t t-elif="agenda.timer_state === 'completed'">Terminé</t>
                                                    <t t-elif="agenda.timer_state === 'overtime'">⚠️ Dépassement</t>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                </t>
                                <t t-else="">
                                    <div class="empty-agenda">
                                        <i class="fa fa-list-ol"/>
                                        <p>Aucun point à l'ordre du jour</p>
                                        <p class="empty-help" t-if="state.session.can_edit_agenda">
                                            Ajoutez des points à l'ordre du jour ci-dessus
                                        </p>
                                    </div>
                                </t>
                            </div>
                            <!-- Documents Section -->
                            <div class="documents-section" t-if="state.session.documents?.length">
                                <div class="actions-header">
                                    <h3>
                                        <i class="fa fa-list"/>
                                        Documents
                                    </h3>
                                </div>
                                <div class="agenda-body">
                                    <table style="width:100%;">
                                        <t t-foreach="state.session.documents" t-as="doc" t-key="doc.id">
                                            <tr class="agenda-item" style="width:100%; display: inline-block;">
                                                <td class="agenda-title" style="width:300px;">
                                                    <t t-esc="doc.name"/>
                                                </td>
                                                <td style="">
                                                    <button class="btn btn-xs btn-primary"
                                                            t-on-click="() => this.downloadDocument(doc)"
                                                            t-if="doc.attachments"
                                                            style="height:35px;">
                                                        <i class="fa fa-download"/>
                                                    </button>
                                                </td>
                                            </tr>
                                        </t>
                                    </table>

                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- Actions Section -->
                    <div class="main-tab-content"
                         t-if="state.activeMainTab === 'actions' and state.session.is_action_assigner">

                        <div class="meeting-actions-section">
                            <div class="actions-header">
                                <h3>
                                    <i class="fa fa-tasks"/>
                                    My Action Items (<t t-esc="state.actions.length"/>)
                                </h3>
                                <div class="actions-header-actions">
                                    <button class="btn-add-action" t-on-click="addNewAction">
                                        <i class="fa fa-plus"/>
                                        Add Action
                                    </button>
                                </div>
                            </div>
                            <div class="actions-body">
                                <div class="actions-list">
                                    <t t-if="state.actions.length">
                                        <t t-foreach="state.actions" t-as="action" t-key="action.id || action_index">
                                            <div class="action-item" t-att-data-status="action.status">
                                                <div class="action-item-header">
                                                    <input
                                                            type="text"
                                                            class="action-title-input"
                                                            t-model="action.name"
                                                            placeholder="Action title..."
                                                            t-on-change="() => updateAction(action)"/>

                                                    <button class="btn-delete-action"
                                                            t-on-click="() => deleteAction(action)">
                                                        <i class="fa fa-trash"/>
                                                    </button>
                                                </div>
                                                <div class="action-item-body">
                                                    <div class="action-fields-grid">
                                                        <div class="action-field">
                                                            <label>
                                                                <i class="fa fa-briefcase"/>
                                                                Project:
                                                            </label>
                                                            <select
                                                                    t-model="action.project_id"
                                                                    t-on-change="() => updateAction(action)"
                                                                    class="form-select">
                                                                <option value="">Select project...</option>
                                                                <t t-foreach="state.availableProjects" t-as="project"
                                                                   t-key="project.id">
                                                                    <option t-att-value="project.id"
                                                                            t-att-selected="action.project_id == project.id">
                                                                        <t t-esc="project.name"/>
                                                                    </option>
                                                                </t>
                                                            </select>
                                                        </div>
                                                        <div class="action-field">
                                                            <label>
                                                                <i class="fa fa-user"/>
                                                                Assigned to:
                                                            </label>
                                                            <select
                                                                    t-model="action.assignee_id"
                                                                    t-on-change="() => updateAction(action)"
                                                                    class="form-select">
                                                                <option value="">Select assignee...</option>
                                                                <t t-foreach="state.availableAssignees" t-as="assignee"
                                                                   t-key="assignee.id">
                                                                    <option t-att-value="assignee.id"
                                                                            t-att-selected="action.assignee_id == assignee.id">
                                                                        <t t-esc="assignee.name"/>
                                                                    </option>
                                                                </t>
                                                            </select>
                                                        </div>
                                                        <div class="action-field">
                                                            <label>
                                                                <i class="fa fa-calendar"/>
                                                                Due Date:
                                                            </label>
                                                            <input
                                                                    type="date"
                                                                    t-model="action.dead_line"
                                                                    t-on-change="() => updateAction(action)"/>
                                                        </div>
                                                        <div class="action-field">
                                                            <label>
                                                                <i class="fa fa-flag"/>
                                                                Priority:
                                                            </label>
                                                            <select t-model="action.priority"
                                                                    t-on-change="() => updateAction(action)">
                                                                <option value="1">Low</option>
                                                                <option value="0">Normal</option>
                                                                <option value="2">High</option>
                                                                <option value="3">Urgent</option>
                                                            </select>
                                                        </div>
                                                        <div class="action-field">
                                                            <label>
                                                                <i class="fa fa-tasks"/>
                                                                Status:
                                                            </label>
                                                            <select t-model="action.status"
                                                                    t-on-change="() => updateAction(action)">
                                                                <option value="todo">To Do</option>
                                                                <option value="in_progress">In Progress</option>
                                                                <option value="done">Done</option>
                                                            </select>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </t>
                                    </t>
                                    <t t-else="">
                                        <div class="empty-actions">
                                            <i class="fa fa-tasks"/>
                                            <p>No action items yet</p>
                                            <button class="btn btn-primary btn-sm" t-on-click="addNewAction">
                                                <i class="fa fa-plus"/>
                                                Add First Action
                                            </button>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- Notes Section -->
                    <div class="main-tab-content" t-if="state.activeMainTab === 'notes'">
                        <div class="meeting-notes-section">
                            <div class="notes-header">
                                <h3>
                                    <i class="fa fa-file-text-o"/>
                                    My Notes
                                </h3>
                                <div class="notes-actions">
                                    <button class="btn-save-notes" t-on-click="saveNotes">
                                        <i class="fa fa-save"/>
                                        Save
                                    </button>
                                </div>
                            </div>
                            <div class="notes-editor">
                                <textarea
                                        class="notes-textarea"
                                        placeholder="Type your personal notes here..."
                                        t-model="state.notes"/>
                            </div>
                        </div>
                    </div>
                    <!-- PV Section -->
                    <div class="main-tab-content" t-if="state.activeMainTab === 'pv' and state.session.is_pv">
                        <div class="meeting-notes-section">
                            <div class="notes-header">
                                <h3>
                                    <i class="fa fa-file-text-o"/>
                                    Procès-Verbal (PV)
                                </h3>
                                <div class="notes-actions">
                                    <button class="btn-save-notes voice-record-btn"
                                            type="button"
                                            title="🎤 Enregistrement LOCAL (fonctionne hors ligne) + Transcription AssemblyAI. Parlez et le texte s'ajoutera au PV">
                                        <i class="fa fa-microphone"/>
                                    </button>
                                    <button class="btn-save-notes voice-add-separator-btn"
                                            type="button"
                                            title="Ajouter un séparateur visuel entre les enregistrements">
                                        <i class="fa fa-minus"/>
                                        <span>Ajouter Séparation</span>
                                    </button>
                                    <div class="voice-status" style="display:none;"></div>
                                    <!-- Template Selection Buttons -->
                                    <button class="btn-save-notes" t-on-click="startBlankPv"
                                            title="Start with blank PV">
                                        <i class="fa fa-file-o"/>
                                        <span>PV Personnalisé</span>
                                    </button>
                                    <button class="btn-save-notes" t-on-click="loadPvTemplate" title="Load PV template">
                                        <i class="fa fa-file-text"/>
                                        <span>Charger Modèle</span>
                                    </button>
                                    <!-- Save Button -->
                                    <button class="btn-save-notes" t-on-click="savePv">
                                        <i class="fa fa-save"/>
                                        Save
                                    </button>
                                </div>
                            </div>
                            <div class="notes-editor">
                                <textarea
                                        class="notes-textarea pv-textarea"
                                        placeholder="Rédigez votre procès-verbal ici...&#10;&#10;Utilisez 'Charger Modèle' pour un modèle pré-rempli ou 'PV Vierge' pour commencer de zéro."
                                        t-model="state.pv"
                                />
                            </div>
                            <!-- Character/Word Count -->
                            <div class="pv-footer">
                                <div class="pv-stats">
                                    <span class="stat-item">
                                        <i class="fa fa-font"/>
                                        <t t-esc="state.pv.length"/>
                                        caractères
                                    </span>
                                </div>
                                <div class="pv-help">
                                    <i class="fa fa-info-circle"/>
                                    <span>Le PV est automatiquement sauvegardé avec la réunion</span>
                                </div>
                                <div class="pv-help">
                                    <i class="fa fa-info-circle"/>
                                    <span>💡 Vous pouvez faire plusieurs enregistrements successifs - ils s'ajouteront
                                        automatiquement. Utilisez "Ajouter Séparation" pour les délimiter visuellement.
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </main>
            </div>
        </div>
    </t>
</templates>